# Label-Detection 離線部署說明

## 📦 專案說明
這是一個完全離線的 OCR 文字辨識系統，使用 Tesseract OCR 引擎，可在沒有網路連接的電腦上運行。

## ✅ 已包含的組件
- ✓ Python 程式碼 (Text-Detection.py, Label-Detection.py)
- ✓ Tesseract OCR 引擎 (tesseract/tesseract.exe)
- ✓ 所有必要的 DLL 檔案 (52 個檔案，約 156 MB)
- ✓ 英文語言模型 (tesseract/tessdata/eng.traineddata)
- ✓ OCR 模型檔案 (easyocr_models/, OCR model/)
- ✓ 範例輸入資料 (input data/)

## 🖥️ 系統需求
- 作業系統: Windows 7/10/11
- Python: 3.13.7 或更高版本
- 硬碟空間: 至少 500 MB

## 📋 必要的 Python 套件
```
opencv-python==4.12.0.88
numpy==2.2.6
pandas==2.3.3
pytesseract
Pillow
```

## 🚀 部署步驟

### 1. 複製整個專案資料夾
將整個 `Label-Detection` 資料夾複製到目標電腦

### 2. 安裝 Python
在目標電腦上安裝 Python 3.13.7 或更高版本
- 下載: https://www.python.org/downloads/
- 安裝時勾選 "Add Python to PATH"

### 3. 安裝 Python 套件
在專案資料夾中開啟 PowerShell，執行：
```powershell
pip install opencv-python numpy pandas pytesseract Pillow
```

如果目標電腦沒有網路，您需要：
1. 在有網路的電腦上下載 wheel 檔案：
   ```powershell
   pip download opencv-python numpy pandas pytesseract Pillow -d packages
   ```
2. 將 `packages` 資料夾複製到目標電腦
3. 在目標電腦上安裝：
   ```powershell
   pip install --no-index --find-links packages opencv-python numpy pandas pytesseract Pillow
   ```

## 📁 目錄結構
```
Label-Detection/
├── Text-Detection.py          # 文字辨識主程式
├── Label-Detection.py         # 標籤檢測程式
├── tesseract/                 # Tesseract OCR 引擎（完全離線）
│   ├── tesseract.exe          # OCR 執行檔
│   ├── tessdata/              # 語言模型資料
│   │   └── eng.traineddata    # 英文模型
│   └── *.dll                  # 所有必要的動態連結庫
├── input data/                # 輸入資料
│   ├── target/                # 目標圖像
│   └── text/                  # 目標文字列表
└── result/                    # 輸出結果
```

## 🎯 使用方法

### 執行文字辨識
```powershell
python Text-Detection.py
```

### 執行標籤檢測
```powershell
python Label-Detection.py
```

## 📊 輸出結果
程式會在 `result/` 資料夾中產生：
- `text_result_Label.png` - 標註了辨識結果的圖像
- `text_result_Label.json` - JSON 格式的詳細結果
- `text_detection_report.csv` - CSV 格式的檢測報告
- `text_summary_report.json` - 總結報告

## ✅ 驗證部署
執行以下命令確認 Tesseract 正常工作：
```powershell
python Text-Detection.py
```

您應該看到類似以下的輸出：
```
正在初始化 Tesseract OCR...
[OK] Tesseract OCR 初始化成功（使用項目目錄）
[OK] Tesseract 路徑: ...\tesseract\tesseract.exe
[OK] Tessdata 路徑: ...\tesseract\tessdata
[OK] Tesseract 版本: 5.4.0.20240606
```

## 🔧 故障排除

### 問題 1: 找不到 Tesseract
**症狀**: `TesseractNotFoundError: tesseract is not installed`
**解決**: 確認 `tesseract/tesseract.exe` 檔案存在且路徑正確

### 問題 2: 找不到語言資料
**症狀**: `TesseractError: Error opening data file`
**解決**: 確認 `tesseract/tessdata/eng.traineddata` 檔案存在

### 問題 3: DLL 載入失敗
**症狀**: 程式啟動時出現 DLL 錯誤
**解決**: 確認 `tesseract/` 資料夾中包含所有 52 個 DLL 檔案

### 問題 4: Python 套件缺失
**症狀**: `ModuleNotFoundError`
**解決**: 重新安裝必要的 Python 套件（參見步驟 3）

## 📈 效能指標
- 辨識準確率: 100% (12/12 目標文字)
- 平均信心度: 0.54-0.96
- 處理速度: 取決於圖像大小和電腦效能

## 📝 注意事項
1. ✅ 本專案完全離線運作，不需要網路連接
2. ✅ 所有模型和執行檔都包含在專案資料夾中
3. ✅ Tesseract 版本: 5.4.0.20240606（穩定版本）
4. ⚠️ 如需辨識其他語言，需下載對應的 traineddata 檔案並放入 `tesseract/tessdata/`

## 🆘 技術支援
如遇到問題，請檢查：
1. Python 版本是否為 3.13.7 或更高
2. 所有必要的 Python 套件是否已安裝
3. `tesseract/` 資料夾是否完整（tesseract.exe + 52 DLL + tessdata）
4. 輸入圖像路徑是否正確

---
**最後測試日期**: 2025年
**Tesseract 版本**: 5.4.0.20240606
**測試結果**: ✅ 12/12 目標文字檢測成功 (100%)
